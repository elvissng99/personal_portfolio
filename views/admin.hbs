<div>
    {{#if projects.length}}
        <table border="1">
            <caption>Projects</caption>
            <tbody>
                {{#each projects}}
                <tr>
                    <td>{{title}}</td>
                    <td>
                        <a href="/project/{{id}}" type="button">View</a>
                        <a href="/editExperience/{{id}}" type="button">Edit</a>
                        <a href="/deleteExperience/{{id}}" type="button">Delete</a>
                    </td>
                </tr>
                {{/each}}
            </tbody>
        </table>
    {{else}}
    <div>
        <p>NO DATA</p>
    </div>
    {{/if}}
    <div>
        <a href="/addExperience" ><button>Add New Experience</button></a>
    </div>
</div>