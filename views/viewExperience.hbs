<div>
    {{#each experiences}}
        <p>{{title}}</p>
        <p>{{description}}</p>
    {{/each}}
    
    {{#if session.email}}
        <a href="/admin"><button>Back</button></a>
    {{else}}
        <a href="/"><button>Back</button></a>
    {{/if}}
    
</div>
